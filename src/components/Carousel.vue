<template>
  
  <button @click="addCount()">{{ count }}</button
  ><transition name="fade"> <img :src="showImg()" /> </transition>`
</template>

<script>
export default {
  name: "Home",
  data() {
    return {};
  },
  methods: {
    slideShow() {},
    showImg() {
      while (this.count < 0) {
        this.count += this.liste.length;
      }
      while (this.count > this.liste.length - 1) {
        this.count -= this.liste.length;
      }
      return this.liste[this.count];
    },

    addCount() {
      this.count++;
    },
  },
  mounted() {
    setInterval(this.addCount, 5000);
  },
  computed: {},
};
</script> 

<style scoped>
</style>